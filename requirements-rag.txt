# ChromaDB vector store and BM25 keyword search — required for the
# panda_doc_search and panda_doc_bm25 RAG tools.
# Install with: pip install -r requirements-rag.txt
chromadb>=0.4.0
rank_bm25>=0.2.2
# Bundled modern SQLite for systems where the OS ships SQLite < 3.35.0
# (e.g. RHEL 8/9, AlmaLinux 9, CERN lxplus).  ChromaDB requires >= 3.35.0.
# Platform marker restricts installation to Linux — macOS ships a modern
# enough SQLite and pysqlite3-binary has no macOS wheel.
pysqlite3-binary>=0.5.3; sys_platform == "linux"
